{% render 'partials/head.liquid', title: person.name %}

<link rel="stylesheet" href="/person.css">

<article>
    <h2>{{ person.name }}</h2>
    <section>
        <p>Geboortedatum: {{ person.birthdate | date: "%d-%m-%Y" }}</p>
        <p>Ik zit in team: {{ person.team }}</p>
        <details>
            <summary>Favoriete code-gerelateerde dingen:</summary>
            <ul>
                <li>Favoriet HTML attribuut: {{ person.fav_attribute }}</li>
                <li>Favoriete CSS property: {{ person.fav_property }}</li>
                <li>Favoriete JavaScript feature: {{ person.fav_feature }}</li>
            </ul>
        </details>
        <p> {{ person.bio }} </p>
    </section>
        {% if person.github_handle %}
          <img src="https://github.com/{{ person.github_handle }}.png" alt="{{ person.name }}">
        {% else %}
          <img src="https://upload.wikimedia.org/wikipedia/commons/c/c5/Toyger_cat_transparent.png" alt="Default Avatar {{ person.name }}">
        {% endif %}
</article>

<style>
body {
    background: {{ person.fav_color }};
}
</style>

{% render 'partials/foot.liquid', squads: squads %}